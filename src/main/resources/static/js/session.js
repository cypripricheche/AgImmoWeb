/** =============================================================================================================================
 * Fichier				: session.js
 * Objectif(s)			: Gestion de la page <index.html>
 * ==============================================================================================================================
 */

/** 
 * Création dynamique des liens de la page (contact)
 */
 function initFiltres(){
	lieuSelected();
	bienSelected();
	budgetSelected();
	surfaceSelected();
	piecesSelected();
	submit();
 }
 function submit(){
/*	window.onload = function() {
	  document.getElementById('formFiltres').onsubmit = function() {
	     do what you want with the form 
	
	    // Should be triggered on form submit
	    alert('c est bon');
	    // You must return false to prevent the default form behavior
	    return false;
	  }
 	}*/
 }